# Native

This directory contains the native host and operating-system adapters.

The first implementation is expected to target Windows. Native code owns
operating-system integration and exposes narrow, documented capabilities to
applications through the platform boundary.
